{
  "name": "node-service",
  "version": "1.0.0",
  "description": "A simple Node.js service for the release train pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --passWithNoTests",
    "build": "echo 'Build complete'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
